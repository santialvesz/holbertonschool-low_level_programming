#include "main.h"
#include <ctype.h>
/** _islower - check if c is lowercase
 * Return: Return 1 or 0 in each case
 */
int _islower(int c);

int c;
if( _islower(c) ) 
return(1);
elseif( _islower(c) )
return(0);
}
