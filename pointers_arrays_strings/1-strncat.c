#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * strncat - concatenates funcions
 * @src: element
 * @dest: element
 * @n: element
 * Return: dest
 */

char *_strncat(char *dest, char *src, int n)
}
	int i, e;

	for (e = 0; dest[e] != 0; e++)
	{
	}
	for (i = 0; i < n && src[i] != 0; i++)
	{
		dest[e] = src[i];
			e++;
	}

	return (dest);
}
