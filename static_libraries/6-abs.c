#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integrer
 * @c: - the int
 * Return: always 0
 */
int _abs(int c)
{
int d;

d = abs(c);
return (d);
_putchar('\n');

}

