#include "main.h"
#include <stdio.h>

/**
 * is_prime_number - if it is prime
 * @n: int
 * Return: 1 or 0 in spec case
 */


int is_prime_number(int n)
{
	if (n > 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
