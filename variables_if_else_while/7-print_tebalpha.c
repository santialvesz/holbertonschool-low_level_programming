#include <stdio.h>
/**
 *  * main - Program that prints the alphabet in lowercase and new line.
 *   * Return: return none
 */
int main(void)
{
char c;
for (c = 'z'; c <= 'a'; c++)
{
putchar (c);
}
putchar('\n');
return (0);
}
